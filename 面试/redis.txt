1.aof持久化
    (1)AOF持久化实现
        a.命令追加:服务器端执行写命令时，同时会将写命令追加到aof缓存区中
        b.aof文件的写入与同步:aof缓存区写入aof文件，aof文件定期同步（写到磁盘上）
    (2)aof文件的载入和数据还原:生成伪客户端执行aof文件指令来还原数据
    (3)aof重写
        a.aof重写的实现原理:通过读取数据库中的键值对来实现
        b.aof后台重写:通过子进程重写&aof重写缓存区